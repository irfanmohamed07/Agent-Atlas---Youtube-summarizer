# Summary contract

Return a JSON object with `story`, `executiveSummary`, `bulletSummary`, `keyTakeaways`, `toolsMentioned`, `apisMentioned`, `frameworksMentioned`, `actionItems`, and `watchRecommendation`. All lists contain strings. Mention only items explicitly present in the transcript.
